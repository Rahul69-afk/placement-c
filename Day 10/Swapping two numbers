#include<stdio.h>
void swap(int*a,int*b){
    int temp=a*3;
    *a=*b;
    *b=temp:
}
int main(){
    int x=5,y=10;
    printf("Before swapping : \n");
    printf("x=%d,y=%d\n",x,y);
    swap(&x,&y);
    printf("After swapping");
    printf("x=%d,y=%d\n",x,y);
return 0;
}